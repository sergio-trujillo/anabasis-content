{
  "id": "code-dp1d-longest-increasing-subsequence",
  "type": "code",
  "section": "gca-module-4-algorithms",
  "title": {
    "en": "Length of longest strictly-increasing subsequence",
    "es": "Longitud de la subsecuencia estrictamente creciente más larga"
  },
  "difficulty": "medium",
  "language": "java",
  "statement": {
    "en": "Given `int[] nums`, return the length of its **longest strictly-increasing subsequence** (LIS). The subsequence does **not** need to be contiguous.\n\nSignature: `public int lengthOfLIS(int[] nums)`\n\n**DP state (O(n²) classic):** Let `dp[i]` = length of the longest increasing subsequence that **ends** at index `i`.\n\n**Recurrence:** `dp[i] = 1 + max(dp[j])` for all `j < i` where `nums[j] < nums[i]`. If no such `j` exists, `dp[i] = 1`.\n\n**Answer:** `max(dp[0], dp[1], ..., dp[n-1])`.\n\n**O(n log n) optimisation (Patience Sorting):** Maintain an auxiliary array `tails` where `tails[k]` is the smallest tail element of any increasing subsequence of length `k+1`. For each `nums[i]`, binary-search for the first index in `tails` that is `>= nums[i]` and replace it (or append if larger than all). The length of `tails` at the end is the LIS length. Both approaches give the same answer; the O(n log n) version **composes binary-search** from this same module.\n\n**Examples:**\n- `[10,9,2,5,3,7,101,18]` → `4` (e.g. `[2,3,7,101]` or `[2,5,7,101]`)\n- `[0,1,0,3,2,3]` → `4` (e.g. `[0,1,2,3]`)\n- `[7,7,7,7]` → `1` (strictly increasing — equal elements don't count)\n- `[1]` → `1`\n- `[]` → `0`\n- `[1,2,3,4,5]` → `5` (already sorted)\n- `[5,4,3,2,1]` → `1` (strictly decreasing)\n\n**Complexity:** O(n²) time with the classic DP; O(n log n) time with the Patience Sorting variant.\n\n**Composes:** DP-1D (O(n²) recurrence) + binary-search (O(n log n) Patience Sorting optimisation).",
    "es": "Dado `int[] nums`, devuelve la longitud de su **subsecuencia estrictamente creciente más larga** (LIS, por sus siglas en inglés). La subsecuencia **no** necesita ser contigua.\n\nFirma: `public int lengthOfLIS(int[] nums)`\n\n**Estado de programación dinámica (clásico O(n²)):** Sea `dp[i]` = longitud de la subsecuencia creciente más larga que **termina** en el índice `i`.\n\n**Recurrencia:** `dp[i] = 1 + max(dp[j])` para todo `j < i` donde `nums[j] < nums[i]`. Si no existe tal `j`, `dp[i] = 1`.\n\n**Respuesta:** `max(dp[0], dp[1], ..., dp[n-1])`.\n\n**Optimización O(n log n) (Patience Sorting):** Mantén un arreglo auxiliar `tails` donde `tails[k]` es el elemento final más pequeño de cualquier subsecuencia creciente de longitud `k+1`. Para cada `nums[i]`, haz una búsqueda binaria para encontrar el primer índice en `tails` que sea `>= nums[i]` y reemplázalo (o agrega al final si es mayor que todos). La longitud de `tails` al finalizar es la longitud del LIS. Ambos enfoques dan la misma respuesta; la versión O(n log n) **compone la búsqueda binaria** del mismo módulo.\n\n**Ejemplos:**\n- `[10,9,2,5,3,7,101,18]` → `4` (p. ej. `[2,3,7,101]` o `[2,5,7,101]`)\n- `[0,1,0,3,2,3]` → `4` (p. ej. `[0,1,2,3]`)\n- `[7,7,7,7]` → `1` (estrictamente creciente — los elementos iguales no cuentan)\n- `[1]` → `1`\n- `[]` → `0`\n- `[1,2,3,4,5]` → `5` (ya ordenado)\n- `[5,4,3,2,1]` → `1` (estrictamente decreciente)\n\n**Complejidad:** O(n²) en tiempo con la DP clásica; O(n log n) con la variante Patience Sorting.\n\n**Compone:** programación dinámica 1D (recurrencia O(n²)) + búsqueda binaria (optimización O(n log n) con Patience Sorting)."
  },
  "starterCode": "class Solution {\n    public int lengthOfLIS(int[] nums) {\n        // TODO\n        return 0;\n    }\n}\n",
  "testCode": "import org.junit.jupiter.api.Test;\nimport static org.junit.jupiter.api.Assertions.*;\n\npublic class SolutionTest {\n\n    @Test\n    void classicEight() {\n        // [10,9,2,5,3,7,101,18]: LIS length 4 e.g. [2,3,7,101]\n        assertEquals(4, new Solution().lengthOfLIS(new int[]{10, 9, 2, 5, 3, 7, 101, 18}));\n    }\n\n    @Test\n    void classicSix() {\n        // [0,1,0,3,2,3]: LIS length 4 e.g. [0,1,2,3]\n        assertEquals(4, new Solution().lengthOfLIS(new int[]{0, 1, 0, 3, 2, 3}));\n    }\n\n    @Test\n    void allEqual() {\n        // [7,7,7,7]: strictly increasing — no extension possible, LIS = 1\n        assertEquals(1, new Solution().lengthOfLIS(new int[]{7, 7, 7, 7}));\n    }\n\n    @Test\n    void singleElement() {\n        assertEquals(1, new Solution().lengthOfLIS(new int[]{1}));\n    }\n\n    @Test\n    void emptyArray() {\n        assertEquals(0, new Solution().lengthOfLIS(new int[]{}));\n    }\n\n    @Test\n    void alreadySorted() {\n        // [1,2,3,4,5]: entire array is the LIS\n        assertEquals(5, new Solution().lengthOfLIS(new int[]{1, 2, 3, 4, 5}));\n    }\n\n    @Test\n    void strictlyDecreasing() {\n        // [5,4,3,2,1]: every element is its own LIS of length 1\n        assertEquals(1, new Solution().lengthOfLIS(new int[]{5, 4, 3, 2, 1}));\n    }\n}\n"
}
