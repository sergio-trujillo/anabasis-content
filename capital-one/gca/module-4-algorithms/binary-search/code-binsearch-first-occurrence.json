{
  "id": "code-binsearch-first-occurrence",
  "type": "code",
  "section": "gca-module-4-algorithms",
  "title": {
    "en": "First occurrence of a target in a sorted array with duplicates",
    "es": "Primera ocurrencia de un objetivo en un arreglo ordenado con duplicados"
  },
  "difficulty": "easy",
  "language": "java",
  "statement": {
    "en": "Given a sorted ascending `int[] nums` (may contain duplicates) and an `int target`, return the index of the **first** occurrence of `target`, or `-1` if not found.\n\nSignature: `public int firstOccurrence(int[] nums, int target)`\n\n**Loop invariant:** maintain `lo <= hi` with a `result` variable initialized to `-1`. At each step compute `mid = lo + (hi - lo) / 2`. If `nums[mid] == target`, record `result = mid` (a candidate) and continue searching left by setting `hi = mid - 1` (there may be an earlier occurrence). If `nums[mid] < target`, set `lo = mid + 1`. If `nums[mid] > target`, set `hi = mid - 1`. Return `result` after the loop.\n\n**Key insight:** unlike the classic search, finding target at `mid` does NOT terminate the loop — you keep narrowing left to find the leftmost copy.\n\n**Complexity:** O(log n) time, O(1) space.\n\n**Sentinel:** return `-1` when target is absent.\n\nExamples:\n- `[1,2,2,2,3]` target=2 → `1`\n- `[1,1,1]` target=1 → `0`\n- `[1,2,2,2,3]` target=3 → `4`\n- `[1,2,3]` target=5 → `-1`\n- `[]` target=1 → `-1`",
    "es": "Dado un arreglo `int[] nums` ordenado de forma ascendente (puede contener duplicados) y un `int target`, devuelve el índice de la **primera** ocurrencia del objetivo, o `-1` si no se encuentra.\n\nFirma: `public int firstOccurrence(int[] nums, int target)`\n\n**Invariante de ciclo:** mantén `lo <= hi` con una variable `result` inicializada en `-1`. En cada paso calcula `mid = lo + (hi - lo) / 2`. Si `nums[mid] == target`, registra `result = mid` (un candidato) y sigue buscando hacia la izquierda con `hi = mid - 1` (puede haber una ocurrencia anterior). Si `nums[mid] < target`, asigna `lo = mid + 1`. Si `nums[mid] > target`, asigna `hi = mid - 1`. Devuelve `result` al finalizar el ciclo.\n\n**Idea clave:** a diferencia de la búsqueda clásica, encontrar el objetivo en `mid` NO termina el ciclo — sigues reduciendo hacia la izquierda para encontrar la copia más a la izquierda.\n\n**Complejidad:** O(log n) en tiempo, O(1) en espacio.\n\n**Centinela:** devuelve `-1` cuando el objetivo está ausente.\n\nEjemplos:\n- `[1,2,2,2,3]` target=2 → `1`\n- `[1,1,1]` target=1 → `0`\n- `[1,2,2,2,3]` target=3 → `4`\n- `[1,2,3]` target=5 → `-1`\n- `[]` target=1 → `-1`"
  },
  "starterCode": "import java.util.*;\n\nclass Solution {\n    public int firstOccurrence(int[] nums, int target) {\n        // TODO\n        return 0;\n    }\n}\n",
  "testCode": "import org.junit.jupiter.api.Test;\nimport static org.junit.jupiter.api.Assertions.*;\n\npublic class SolutionTest {\n\n    @Test\n    void multipleDuplicatesInMiddle() {\n        // [1,2,2,2,3] — first 2 is at index 1\n        assertEquals(1, new Solution().firstOccurrence(new int[]{1, 2, 2, 2, 3}, 2));\n    }\n\n    @Test\n    void allElementsSame() {\n        // [1,1,1] — first 1 is at index 0\n        assertEquals(0, new Solution().firstOccurrence(new int[]{1, 1, 1}, 1));\n    }\n\n    @Test\n    void targetAtLastPosition() {\n        // [1,2,2,2,3] — first 3 is at index 4\n        assertEquals(4, new Solution().firstOccurrence(new int[]{1, 2, 2, 2, 3}, 3));\n    }\n\n    @Test\n    void targetNotPresent() {\n        assertEquals(-1, new Solution().firstOccurrence(new int[]{1, 2, 3}, 5));\n    }\n\n    @Test\n    void emptyArray() {\n        assertEquals(-1, new Solution().firstOccurrence(new int[]{}, 1));\n    }\n\n    @Test\n    void noDuplicatesTargetFound() {\n        // Works correctly on arrays without duplicates too\n        assertEquals(2, new Solution().firstOccurrence(new int[]{10, 20, 30, 40, 50}, 30));\n    }\n\n    @Test\n    void targetAppearsAtStart() {\n        // Duplicates spanning from index 0\n        assertEquals(0, new Solution().firstOccurrence(new int[]{5, 5, 5, 7, 9}, 5));\n    }\n}\n"
}
