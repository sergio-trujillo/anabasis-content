{
  "id": "code-encdec-luhn-check-digit",
  "type": "code",
  "section": "gca-module-3-implementation",
  "title": {
    "en": "Compute the Luhn check digit for a partial card number",
    "es": "Calcular la cifra de control Luhn para un número de tarjeta parcial"
  },
  "difficulty": "medium",
  "language": "java",
  "statement": {
    "en": "Given a `String partial` of digit characters (length >= 1), return the single check digit `0–9` that — when **appended** to `partial` — makes the resulting full number pass the **Luhn checksum**.\n\nSignature: `public int luhnCheckDigit(String partial)`\n\n**Luhn algorithm (applied to the full number after appending the check digit `d`):**\n1. Starting from the **rightmost** digit (the check digit `d`) and moving left, mark positions as *odd* (1st, 3rd, 5th, …) and *even* (2nd, 4th, …) from the right.\n2. For **odd** positions: take the digit as-is.\n3. For **even** positions: double the digit. If the result is > 9, subtract 9.\n4. Sum all values. The number is valid if and only if `total % 10 == 0`.\n\n**Computing the check digit from `partial`:**\nLet `S` = the Luhn sum of the digits in `partial`, computed as if `partial` is the left portion and the check digit will be added at the right (i.e., digits in `partial` occupy the *even* and *odd* positions starting from position 2 from the right). Then:\n```\ncheckDigit = (10 - (S % 10)) % 10\n```\n\nThis is distinct from run-length encoding: the scheme derives its output via modular arithmetic over a weighted sum, not a symbol-mapping table.\n\nInput constraints: `partial` consists only of digit characters (`'0'–'9'`), length >= 1.\n\nExamples:\n- `\"7992739871\"` → `3` (classic Luhn textbook example; the full number `\"79927398713\"` passes Luhn)\n- `\"0\"` → `0` (trivial: `0` appended gives `\"00\"`, sum = 0, passes)\n- `\"1\"` → `8` (full number `\"18\"`: double 1 → 2, add 8 → 10, passes)",
    "es": "Dada una `String partial` de caracteres dígito (longitud >= 1), devuelve la única cifra de control `0–9` que — al **agregarse** al final de `partial` — hace que el número completo pase la **suma de verificación Luhn**.\n\nFirma: `public int luhnCheckDigit(String partial)`\n\n**Algoritmo de Luhn (aplicado al número completo tras agregar la cifra de control `d`):**\n1. Partiendo desde el dígito más a la derecha (la cifra de control `d`) hacia la izquierda, marca las posiciones como *impares* (1ª, 3ª, 5ª, …) y *pares* (2ª, 4ª, …) desde la derecha.\n2. Posiciones **impares**: toma el dígito tal cual.\n3. Posiciones **pares**: duplica el dígito. Si el resultado es > 9, réstale 9.\n4. Suma todos los valores. El número es válido si y solo si `total % 10 == 0`.\n\n**Cálculo de la cifra de control a partir de `partial`:**\nSea `S` la suma Luhn de los dígitos en `partial`, calculada como si `partial` fuera la parte izquierda y la cifra de control se agregara a la derecha (es decir, los dígitos de `partial` ocupan las posiciones *pares* e *impares* comenzando desde la posición 2 desde la derecha). Entonces:\n```\ncifraControl = (10 - (S % 10)) % 10\n```\n\nEste esquema es distinto de la codificación por longitud de ejecución: el resultado se deriva mediante aritmética modular sobre una suma ponderada, no mediante una tabla de mapeo de símbolos.\n\nRestricciones de entrada: `partial` contiene solo caracteres dígito (`'0'–'9'`), longitud >= 1.\n\nEjemplos:\n- `\"7992739871\"` → `3` (ejemplo canónico Luhn; el número completo `\"79927398713\"` pasa la verificación)\n- `\"0\"` → `0` (trivial: agregar `0` da `\"00\"`, suma = 0, pasa)\n- `\"1\"` → `8` (número completo `\"18\"`: duplicar 1 → 2, más 8 → 10, pasa)"
  },
  "starterCode": "import java.util.*;\n\nclass Solution {\n    public int luhnCheckDigit(String partial) {\n        // TODO\n        return 0;\n    }\n}\n",
  "testCode": "import org.junit.jupiter.api.Test;\nimport static org.junit.jupiter.api.Assertions.*;\n\npublic class SolutionTest {\n\n    @Test\n    void luhnTextbookExample() {\n        // Classic Luhn reference: \"7992739871\" -> 3, full \"79927398713\" passes\n        assertEquals(3, new Solution().luhnCheckDigit(\"7992739871\"));\n    }\n\n    @Test\n    void trivialZero() {\n        // Single digit \"0\" -> check digit 0, full \"00\" passes Luhn\n        assertEquals(0, new Solution().luhnCheckDigit(\"0\"));\n    }\n\n    @Test\n    void singleDigitOne() {\n        // \"1\" -> check digit 8, full \"18\" passes: double(1)=2, add 8 -> 10\n        assertEquals(8, new Solution().luhnCheckDigit(\"1\"));\n    }\n\n    @Test\n    void visaStylePartial() {\n        // Partial Visa-style number; check digit must make it pass\n        int check = new Solution().luhnCheckDigit(\"453914880343646\");\n        assertTrue(check >= 0 && check <= 9);\n        // Verify the full number actually passes Luhn\n        String full = \"453914880343646\" + check;\n        assertTrue(luhnPasses(full), \"Full number should pass Luhn\");\n    }\n\n    @Test\n    void roundTripVerification() {\n        // For any partial, appending the check digit must yield a Luhn-valid number\n        String partial = \"7992739871\";\n        int check = new Solution().luhnCheckDigit(partial);\n        assertTrue(luhnPasses(partial + check));\n    }\n\n    @Test\n    void allNinesPartial() {\n        int check = new Solution().luhnCheckDigit(\"9999999999\");\n        assertTrue(luhnPasses(\"9999999999\" + check));\n    }\n\n    // Helper: verify a complete number passes Luhn\n    private boolean luhnPasses(String number) {\n        int sum = 0;\n        boolean doubleIt = false;\n        for (int i = number.length() - 1; i >= 0; i--) {\n            int d = number.charAt(i) - '0';\n            if (doubleIt) {\n                d *= 2;\n                if (d > 9) d -= 9;\n            }\n            sum += d;\n            doubleIt = !doubleIt;\n        }\n        return sum % 10 == 0;\n    }\n}\n"
}
