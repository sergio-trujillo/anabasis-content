{
  "id": "code-histbucket-bucket-sort-strings",
  "type": "code",
  "section": "gca-module-2-data-manipulation",
  "title": {
    "en": "Stable bucket sort by string length",
    "es": "Ordenamiento estable por cubetas según longitud de cadena"
  },
  "difficulty": "medium",
  "language": "java",
  "statement": {
    "en": "Given a `String[]` array `words` of lowercase letters with lengths in `[1, maxLen]` and the integer `maxLen`, return a **new** `String[]` sorted by **length ascending**. Words of the same length must appear in their **original input order** (stable sort).\n\nUse a `List<String>[]` bucket array of size `maxLen + 1`: add each word to `buckets[word.length()]`, then flatten the non-empty buckets in index order.\n\nDo NOT use `Arrays.sort` or `Comparator`.\n\nExamples:\n- `[\"banana\", \"ok\", \"a\", \"no\", \"apple\"]` maxLen=6 → `[\"a\", \"ok\", \"no\", \"apple\", \"banana\"]`\n- `[\"hi\"]` maxLen=5 → `[\"hi\"]`\n- `[]` maxLen=3 → `[]`\n\nSignature: `public String[] bucketSortByLength(String[] words, int maxLen)`",
    "es": "Dado un arreglo `String[]` llamado `words` de letras minúsculas con longitudes en `[1, maxLen]` y el entero `maxLen`, devuelve un **nuevo** `String[]` ordenado por **longitud ascendente**. Las palabras de la misma longitud deben aparecer en su **orden original de entrada** (ordenamiento estable).\n\nUsa un arreglo de cubetas `List<String>[]` de tamaño `maxLen + 1`: agrega cada palabra a `buckets[word.length()]`, luego aplana las cubetas no vacías en orden de índice.\n\nNO uses `Arrays.sort` ni `Comparator`.\n\nEjemplos:\n- `[\"banana\", \"ok\", \"a\", \"no\", \"apple\"]` maxLen=6 → `[\"a\", \"ok\", \"no\", \"apple\", \"banana\"]`\n- `[\"hi\"]` maxLen=5 → `[\"hi\"]`\n- `[]` maxLen=3 → `[]`\n\nFirma: `public String[] bucketSortByLength(String[] words, int maxLen)`"
  },
  "starterCode": "import java.util.*;\n\nclass Solution {\n    public String[] bucketSortByLength(String[] words, int maxLen) {\n        // TODO\n        return new String[]{};\n    }\n}\n",
  "testCode": "import org.junit.jupiter.api.Test;\nimport static org.junit.jupiter.api.Assertions.*;\nimport java.util.*;\n\npublic class SolutionTest {\n\n    @Test\n    void classic() {\n        String[] result = new Solution().bucketSortByLength(\n            new String[]{\"banana\", \"ok\", \"a\", \"no\", \"apple\"}, 6);\n        assertArrayEquals(new String[]{\"a\", \"ok\", \"no\", \"apple\", \"banana\"}, result);\n    }\n\n    @Test\n    void stable_sameLength() {\n        // \"ok\" appears before \"no\" in input; both length 2 — stable sort must preserve that order\n        String[] result = new Solution().bucketSortByLength(\n            new String[]{\"ok\", \"no\", \"a\"}, 3);\n        assertArrayEquals(new String[]{\"a\", \"ok\", \"no\"}, result);\n    }\n\n    @Test\n    void singleWord() {\n        String[] result = new Solution().bucketSortByLength(new String[]{\"hi\"}, 5);\n        assertArrayEquals(new String[]{\"hi\"}, result);\n    }\n\n    @Test\n    void empty() {\n        String[] result = new Solution().bucketSortByLength(new String[]{}, 3);\n        assertArrayEquals(new String[]{}, result);\n    }\n\n    @Test\n    void allSameLength() {\n        // every word goes into the same bucket; original order preserved\n        String[] result = new Solution().bucketSortByLength(\n            new String[]{\"cat\", \"dog\", \"ant\"}, 3);\n        assertArrayEquals(new String[]{\"cat\", \"dog\", \"ant\"}, result);\n    }\n\n    @Test\n    void alreadySortedByLength() {\n        String[] result = new Solution().bucketSortByLength(\n            new String[]{\"a\", \"ab\", \"abc\", \"abcd\"}, 4);\n        assertArrayEquals(new String[]{\"a\", \"ab\",\"abc\", \"abcd\"}, result);\n    }\n\n    @Test\n    void longestFirst() {\n        // input in reverse length order\n        String[] result = new Solution().bucketSortByLength(\n            new String[]{\"abcde\", \"abcd\", \"abc\", \"ab\", \"a\"}, 5);\n        assertArrayEquals(new String[]{\"a\", \"ab\", \"abc\", \"abcd\", \"abcde\"}, result);\n    }\n}\n"
}
